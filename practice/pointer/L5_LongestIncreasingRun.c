/*
Level 5) Longest Strictly-Increasing Run (Contiguous)

เป้าหมาย
หา “ช่วงต่อเนื่อง” ที่สมาชิก เพิ่มขึ้นแบบเข้มงวด (strict: a[i] < a[i+1]) ที่ยาวที่สุด
รายงาน: ความยาวช่วง best_len และ ดัชนีเริ่ม start_index.

เงื่อนไข/แนะแนว
ใช้พอยน์เตอร์วิ่งหนึ่งตัวและ “จุดเริ่มช่วง” อีกตัว: int *start = arr;
เดิน p ตั้งแต่ arr ถึง arr + n - 2:
ถ้า *p < *(p+1) → อยู่ในช่วงเพิ่มขึ้นต่อ
ถ้าไม่ → ปิดช่วงเดิม คำนวณ len = (int)((p - start) + 1) แล้วอัปเดต best หากดีกว่า จากนั้นเริ่มช่วงใหม่ที่ start = p + 1
อย่าลืมเช็คช่วงสุดท้ายหลังจบลูป
start_index = (int)(best_start - arr)

ตัวอย่าง
arr = 3 4 5 2 9 10 11 1
→ best_len = 4, start_index = 0   (ช่วง 3,4,5 | แล้ว 2,9,10,11 ยาว 4 เริ่มที่ index 3)
*/